// Copie ce fichier en lib/env.dart et remplis les valeurs.
// lib/env.dart est gitignorée — ne jamais committer les vraies clés.
class Env {
  static const useFirebase  = true;
  static const mistralApiKey = 'REMPLACE_PAR_TA_CLE_MISTRAL';
}
